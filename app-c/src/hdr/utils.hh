#ifndef UTILS_HH
#define UTILS_HH

#include "../hdr-locked/my-types.hh"
#include <stdio.h>

void print(i8 const *const, u32);

void print(i8 const *const, i8 const *const);

void print(i8 const *const, u32, i8 const *const);

#endif
